[前端速通](https://www.yuque.com/leifengyang/sutong/knn21ltyewyv6l7o#o2XdL)    

# ES6


let 关键字
推荐使用let关键字，var 存在诸多问题。
越域
重复声明
变量提升
const

链判断

```js
const firstName = message?.body?.user?.firstName || 'default';
```

箭头函数

数组结构
对象结构

参数默认值

箭头函数
```js
let sum2 = (a, b) => a + b;
```

模版字符串
```js
let info = "你好，我的名字是：【"+name+"】，年龄是：【"+age+"】，邮箱是：【】"
console.log(info);

# 模板字符串的写法
let info = `你好，我的名字是：${name}，年龄是：${person.age}，邮箱是：${person.email}`
console.log(info);
```


前端 模块化
方法一
```js
export function list() {  
    console.log('list....')  
}  
  
export function add() {  
    console.log('add....')  
}
```

```javascript
//引入  
import {list,add} from "./1.js"  
//调用  
list()  
add()
```

方法二

```js
// 一个文件里只能有一个默认导出
export default {  
    getList() {  
        console.log('获取数据列表2')  
    },  
    save() {  
        console.log('保存数据2')  
    }  
}
```

```js
//引入  
import m from "./01.js"  
//调用  
m.getList()  
m.save()
```


# Promise

```js
function fetchData() {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      const success = true; // 模拟成功或失败
      if (success) {
        resolve('Data received');
      } else {
        reject('Failed to fetch data');
      }
    }, 2000);
  });
}

fetchData()
  .then((data) => {
    console.log(data); // "Data received"
  })
  .catch((error) => {
    console.error(error); // "Failed to fetch data"
  });

```